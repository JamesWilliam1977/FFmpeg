interface:
  display_name: "FFmpeg"
  short_description: "Repo-specific patterns and workflows for FFmpeg"
  default_prompt: "Use the FFmpeg repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true